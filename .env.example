# Environment Variables for MCP LP One-Pager Tools

# Database Connection
DB_URL=postgres://mcp:mcp@localhost:5433/mcp_ctx

# Embedding Model (optional, default: Xenova/all-MiniLM-L6-v2 - local model, no API key needed)
# Uses local embeddings via @xenova/transformers - no API key required!
EMBEDDING_MODEL=Xenova/all-MiniLM-L6-v2

OPENAI_API_KEY=
